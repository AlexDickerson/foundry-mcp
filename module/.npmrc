# foundry-vtt-types peer-requires typescript@^5.4 but the project pins ^6.0.3.
# Both versions type-check this code fine; the peer constraint is stale.
# Revert to npm 6 peer-dep handling so install works without --legacy-peer-deps.
legacy-peer-deps=true
